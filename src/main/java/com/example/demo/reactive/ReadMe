

WebFlux ===================
remove configuration with @EnableWebSecurity and add new one with @EnableWebFluxSecurity





public static void main(String[] args) {
		
		SpringApplication app = new SpringApplication(SpringIaApplication.class);
        app.setWebApplicationType(WebApplicationType.REACTIVE);
        app.run(args);
        //SpringApplication.run(SpringIaApplication.class, args);
}

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-webflux</artifactId>
</dependency>

WebFlux end ==================



<dependency>
    <groupId>io.projectreactor</groupId>
    <artifactId>reactor-core</artifactId>
</dependency>	

<dependency>
    <groupId>io.projectreactor</groupId>
    <artifactId>reactor-test</artifactId>
    <scope>test</scope>
</dependency>


Operation can be categorized: Creation, Combination, Transformation and logic


